# finance-anomaly-detection
AI-based financial anomaly detection system using LLM embeddings and stacking ensemble (XGBoost + LightGBM). It processes high-dimensional ledger data, improves feature representation, handles imbalanced datasets, and detects fraud with higher accuracy and reduced false positives.
